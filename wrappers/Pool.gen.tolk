// Auto-generated wrapper for contract 'Pool'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/deployment-types"
import "@contracts/pool-types"

/// Storage `Storage` is defined in `@contracts/pool-types`
struct Pool {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun Pool.fromStorage(storage: Storage, toShard: AddressShardingOptions? = null): Pool {
    val stateInit = ContractState {
        code: build("Pool"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return Pool { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun Pool.fromAddress(address: address): Pool {
    return Pool { address };
}

/// Deploys the contract to the blockchain
fun Pool.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendConfirmPoolDeployment(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ConfirmPoolDeployment { queryId },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendInitWalletBinding(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: InitWalletBinding { queryId },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendTakeWalletAddress(
    self,
    from: address,
    queryId: uint64,
    walletAddress: address,
    rest: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TakeWalletAddress { queryId, walletAddress, rest },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    fromUser: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonTransferNotification { queryId, amount, fromUser, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendWithdraw(
    self,
    from: address,
    clientQueryId: uint64,
    root: uint256,
    nullifierHash: uint256,
    recipient: address,
    proof: ZkProof,
    nullifierSetProof: PoolSparseSetUpdateProof,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Withdraw {
            clientQueryId,
            root,
            nullifierHash,
            recipient,
            proof: proof.toCell(),
            nullifierSetProof: nullifierSetProof.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Pool.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Pool.jettonMaster(self): address {
    return net.runGetMethod(self.address, "jettonMaster");
}

fun Pool.factory(self): address {
    return net.runGetMethod(self.address, "factory");
}

fun Pool.jettonWallet(self): address? {
    return net.runGetMethod(self.address, "jettonWallet");
}

fun Pool.denomination(self): coins {
    return net.runGetMethod(self.address, "denomination");
}

fun Pool.currentRoot(self): uint256 {
    return net.runGetMethod(self.address, "currentRoot");
}

fun Pool.nextIndex(self): uint32 {
    return net.runGetMethod(self.address, "nextIndex");
}

fun Pool.relayerReserve(self): coins {
    return net.runGetMethod(self.address, "relayerReserve");
}

fun Pool.rentRunway(self): coins {
    return net.runGetMethod(self.address, "rentRunway");
}

fun Pool.sparseRoot(self, domain: uint32, bucketId: uint8): uint256 {
    return net.runGetMethod(self.address, "sparseRoot", [domain, bucketId]);
}

fun Pool.withdrawalCount(self): uint32 {
    return net.runGetMethod(self.address, "withdrawalCount");
}

fun Pool.treeDepth(self): int {
    return net.runGetMethod(self.address, "treeDepth");
}

fun Pool.previewInsertProof(
    self,
    oldRoot: uint256,
    newRoot: uint256,
    commitment: uint256,
    leafIndex: uint32,
    proofA: slice,
    proofB: slice,
    proofC: slice,
): bool {
    return net.runGetMethod(
        self.address,
        "previewInsertProof",
        [
            oldRoot,
            newRoot,
            commitment,
            leafIndex,
            proofA,
            proofB,
            proofC,
        ],
    );
}

fun Pool.previewWithdrawProof(
    self,
    root: uint256,
    nullifierHash: uint256,
    recipientField: uint256,
    proofA: slice,
    proofB: slice,
    proofC: slice,
): bool {
    return net.runGetMethod(
        self.address,
        "previewWithdrawProof",
        [
            root,
            nullifierHash,
            recipientField,
            proofA,
            proofB,
            proofC,
        ],
    );
}
