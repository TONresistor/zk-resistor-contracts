{
  "version": 1,
  "waivers": [
    {
      "contract": "Pool",
      "source_path": "contracts/pool.tolk",
      "rule_name": "replace_if_condition_with_false",
      "line_contains": "if (payloadIsExotic) {",
      "reason": "All valid TON exotic cell layouts fail the exact 608-bit/2-ref DepositPayload shape check that follows. The early guard keeps untrusted exotic input out of typed parsing."
    },
    {
      "contract": "Pool",
      "source_path": "contracts/recipient-binding.tolk",
      "rule_name": "remove_assert",
      "line_contains": "assert (recipientField != 0) throw Errors.InvalidRecipient;",
      "reason": "Triggering this guard requires a 248-bit preimage for the zero binding. Without the guard, the withdrawal circuit still rejects recipient=0 before any nullifier state write; the guard only provides a clearer error for a computationally infeasible input."
    }
  ]
}
